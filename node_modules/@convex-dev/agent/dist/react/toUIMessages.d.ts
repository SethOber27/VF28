import type { UIMessage as AIUIMessage } from "ai";
import type { MessageDoc } from "../client/index.js";
import type { MessageStatus } from "../validators.js";
export type UIMessage = AIUIMessage & {
    key: string;
    order: number;
    stepOrder: number;
    status: "streaming" | MessageStatus;
    agentName?: string;
};
export declare function toUIMessages(messages: (MessageDoc & {
    streaming?: boolean;
})[]): UIMessage[];
//# sourceMappingURL=toUIMessages.d.ts.map